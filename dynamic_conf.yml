http:
  middlewares:
    secure-headers:
      headers:
        frameDeny: true
        sslRedirect: true
        stsSeconds: 31536000

tls:
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: true
